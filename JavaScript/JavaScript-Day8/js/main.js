/* let hanoi = function(discs,src,aux,dst){
    if(disc >0) {
        hanoi(disc -1, src, dst, aux);
        console.log(`Move Disc: ${disc} from ${scr} to ${dst}`);
        hanoi(disc -1, src, dst, aux);
    }
}
hanoi(3, 'Post 1', 'Post 2', 'Post 3') */